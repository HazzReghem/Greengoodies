<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Greengoodies{% endblock %}</title>

        <link rel="stylesheet" href="{{ asset('build/css/app.min.css') }}">
        <link rel="preload" as="image" href="{{ asset('images/logo.webp') }}">

    </head>

    <body>
        <header>
            <nav>
                <a href="{{ path('home') }}" id="logo">
                    <img src="{{ asset('images/logo.webp') }}" alt="Greengoodies">
                </a>
                <ul id="menu">
                    <li><a href="{{ path('products') }}">Nos produits</a></li>
                    <li><a href="{{ path('cart') }}">Mon panier</a></li>
                    <li><a href="{{ path('account') }}">Mon compte</a></li>
                    <li><a href="{{ path('contact') }}">Déconnexion</a></li>
                </ul>
            </nav>
        </header>

        <main>
            {% block body %}{% endblock %}
        </main>

        <footer>
            <p>>© GreenGoodies - {{ "now"|date("Y") }}</p>
        </footer>
    </body>
</html>
